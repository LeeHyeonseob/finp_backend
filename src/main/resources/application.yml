spring:
  application:
    name: finp_backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/finp?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1038
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true

